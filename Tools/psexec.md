
# impacket
If psexec is hung on starting up the service, it may mean that Windows Defender Firewall or an AV is blocking it from starting.


# msfconsole psexec
If psexec doesn't start the service, try to specify the SMBshare in the payload options to ADMIN$ or C$
